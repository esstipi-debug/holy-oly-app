# HANDOFF — Holy Oly: SP5 autorregulación / vigencia de RM (sesión 2026-06-10 b)

- **Fecha:** 2026-06-10 (misma jornada que el handoff heatmap+anclaje — este lo complementa)
- **Para:** retomar en otra ventana sin perder contexto. Complementa `HANDOFF-GO-LIVE.md` y
  `HANDOFF-2026-06-10-heatmap-y-anclaje.md`.

---

## 0. Resumen en una línea

**SP5 SHIPPED a la rama (y FF a main local, sin push):** el coach actualiza `Plan.rms` a mitad de
ciclo SIN re-instanciar (el kg derivado recae solo), historial append-only (`RmUpdate`, migración
**16**), detección de PRs (estricto `>`, el coach confirma y entra el valor final) y vigencia
("fijado hace N sem") en la sección **RMs** nueva del drill-down. Reviews de dominio (El Carnicero)
y React aplicados. Instancia del owner `:8765` relanzada con el build SP5.

## 1. Estado git (CRÍTICO)

| Hecho | Detalle |
|---|---|
| Rama worktree | `claude/nifty-lumiere-6f1195` — worktree `C:\Holy Oly 0017\.claude\worktrees\nifty-lumiere-6f1195` |
| Commits SP5 | `f7c264b` plan · `8bc563c` core tipos/schemas · `5713d8b` core logic · `d2cd32f` migración 16 · `5481c2b` api · `9f88759` repos web · `9094a21` RmSection/RmEditSheet · `d78a492` drill-down · `d340eab` fixes de reviews · (+ este handoff) |
| `main` local | FF desde la rama (ver §5) — **SIN push** (regla local-only) |
| **WIP ajeno** | El checkout principal sigue con el **booking sin commitear** (migración `15_booking` etc.). NO tocarlo, NO commitearlo. Sobrevive el stash-dance. |
| Migraciones | Commiteadas: 0–14 + **16_rm_history** (la 15 la tiene el WIP booking → **cuando el booking se commitee, renumerar a 17** o aceptar el orden 16<15… mejor renumerar) |

## 2. Qué se construyó (SP5)

Spec: `docs/superpowers/specs/2026-06-05-sp5-autorregulacion-rm-design.md` (+deltas 06-08) ·
Plan: `docs/superpowers/plans/2026-06-10-sp5-autorregulacion-rm.md` (T1–T9, TDD).

- **Core** (`logic/rm.ts`): `prCandidates(actuals, rms)` — PR = set HECHO con kg **estrictamente >**
  RM del `rmRef` del movimiento; variantes flaggeadas (muestran el movimiento, el coach juzga);
  ≤1 por lift (el mayor; empate → semana más reciente); **`doneAt`** viaja en el candidato (fecha
  real, `week` queda de fallback). `rmVigencia(history, fallback, today)` — última `RmUpdate` por
  lift; sin historial → `plan.startDate` (planes pre-SP5); sin nada → `{}`. JAMÁS rep-max.
- **API**: tabla `RmUpdate` (mig 16, append-only, cascade on delete) · `PUT /athletes/:id/rms`
  (1..4 lifts sin duplicados, reason manual|pr; transaccional, **NO re-instancia** → ediciones del
  coach sobreviven, kg recae al leer) · `GET .../pr-candidates` · `GET .../rm-history` — los 3 bajo
  el guard coach+Vínculo (+402 gate en el write) · `savePlan` siembra 4 baselines `reason:"assign"`
  con **`setAt = HOY`** (la fecha del acto — ver §3) · **export D3 incluye `rmUpdates`**.
- **Web**: `Repository.updateRms/getPrCandidates/getRmHistory` (Http valida con Zod; Local espeja
  incl. rechazo sin plan) · `RmSection` (4 RMs + vigencia + hint stale ≥12 sem con explicación +
  PRs por confirmar con fecha real) · `RmEditSheet` (manual = 4 lifts, pr = 1 lift precargado; el
  copy dice explícito que el 1RM lo pone el coach) · `Drilldown` recarga plan + remonta mapa/sesiones
  tras cada update (`rmsStamp`).

## 3. Reviews (T8) — qué cambió y qué se decidió NO cambiar

**El Carnicero** (0 CRITICAL): aplicados — export D3 con `rmUpdates` (HIGH) · **baseline con
`setAt=HOY`** (HIGH: con anclaje por compe el startDate cae en el pasado → retro-fechar era
falso-stale y rompía el invariante al re-asignar; el spec §3.1 decía `startDate ?? today` y se
**superó a conciencia**) · error de carga → vigencia "—" (no fallback) · hint stale con contexto
HR-2 · `PrCandidate.doneAt` · **rulebook §Semáforo reconciliado** (semáforo = worse-of(ACWR,
recuperación); vigencia 1RM = señal coach-only, cablearla al monitor sería diseño aparte).
**React** (0 CRITICAL): guard de unmount + `useCallback`/`useMemo` + loading `role="status"` en
`RmSection` · draft del sheet sólo se re-arma al abrir + ref anti doble-submit · `onRmsChange`
estable directo · aria-labels ("Editar RMs", section como region).

**NO aplicado (consciente, no re-litigar):** filtrar candidatos por `startDate` (la evidencia es
evidencia; el coach juzga) · "descartar PR" (fuera de v1 por spec) · cablear vigencia al semáforo
(diseño aparte) · migrar tests a `userEvent` (el codebase entero usa `fireEvent`) · `key` por índice
en las cards de PR (lift es identidad estable; core garantiza ≤1/lift).

## 4. Verificación

`pnpm -r typecheck` ✓ · core **189** · web **291** · api **55 unit + 68 int** (verify, PG embebido;
`rm.int.test.ts`: cascada+prescripción intacta, PR estricto+auto-resolución, authz 401/401/403,
400/404, baselines hoy) · `pnpm lint` 0 errors (warning preexistente `email/index.ts`). **Smoke en
vivo** (instancia aislada :8766, borrada): migración 16 al boot ✓ · PUT rms → cascada visible en
sesiones (70%×155=109) ✓ · RmSection en DOM con vigencia mixta (real + fallback pre-SP5) ✓ · sheet ✓
· **atleta: cero RM / cero RPE** ✓.

## 5. Instancia del owner (`:8765`) — relanzada con SP5

- Estado persistente `C:\HolyOlyDemo-heat` (PG `:5440`) — **los datos del owner sobreviven**, la
  migración 16 aplicó encima. Identidades de siempre (`/auth/local-demo-login?as=coach|atleta`).
- ⚠ **Planes asignados ANTES de SP5 no tienen baselines** → su vigencia muestra el fallback al
  `startDate` ("fijado hace N sem" puede salir alto si el plan se ancló por compe). Es el caso
  pre-SP5 honesto por diseño; re-asignar o editar RMs empieza el historial real.
- Rebuild: receta idéntica al handoff heatmap §4 pero con worktree **nifty-lumiere-6f1195**.
- Limpieza pendiente menor: borrar a mano `C:\HolyOlyDemo-sp5-smoke` (dir del smoke, procesos ya
  muertos; el guard del shell no permite borrarlo desde acá).

## 6. Roadmap + decisiones vigentes (sin cambios de fondo)

1. **Bugs que reporte el owner** sobre `:8765` (ahora caza con SP5 adentro).
2. **Motor Prilepin core dormant** — LEER ANTES `docs/superpowers/specs/2026-06-09-bundle-reconciliacion-vs-holyoly.md`
   (sin sRPE, rounding 1 kg, TDD; el pseudocódigo del bundle tiene bugs). Luego readiness→modulación
   → peaking/olas → app-viva.
3. Contenido: recetas sesión-por-sesión para los otros 23 macros (sólo ruso-5d tiene).
4. Pendientes previos: adapter MP real, email real, legal, i18n (parqueado prioritario), booking WIP (mig → 17).
5. **Ciclo menstrual (pregunta del owner 06-10):** hoy existe TODA la cañería (consent, cifrado
   at-rest mig 12, redacción server-side `redactCycle` — el coach sólo ve `{share,inLutealNow,health,reliable}`,
   endpoints coach+me, `Repository.getCycleShare/getCycleContext` en ambas implementaciones, export
   D3 crudo para la atleta) pero **ninguna pantalla lo consume** (sólo el copy legal lo menciona) y
   **no adapta nada** — por rulebook §3 el ciclo NUNCA dispara el semáforo, sólo contextualiza. La
   "inteligencia para adaptar" es la fase **readiness→modulación** del roadmap (post-motor), y la
   UI del ciclo (atleta opt-in + card redactada del coach) es un slice propio pendiente de diseño.

## 7. Gotchas de esta sesión

- Worktree nuevo = `pnpm install` + `prisma generate` (node_modules por-worktree).
- `pnpm -r lint` NO existe — el lint es **`pnpm lint`** en la raíz (`eslint .`).
- Guard del shell: bloquea `git commit -m` con ciertos textos (usar `-F archivo`) y `Remove-Item`
  sobre dirs top-level de `C:\`.
- El runner demo: `HOLYOLY_NO_BROWSER=1` evita que abra ventana; el lock-file hace que un segundo
  runner sólo abra ventana si el stack está sano.
- El seed NO pasa por `savePlan` → instancias seedeadas arrancan sin historial de RMs (fallback).
