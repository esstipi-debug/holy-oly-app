# Copy to apps/api/.env and adjust. Matches docker-compose.yml (port 5433).
DATABASE_URL="postgresql://holyoly:holyoly@localhost:5433/holyoly_dev?schema=public"
TEST_DATABASE_URL="postgresql://holyoly:holyoly@localhost:5433/holyoly_test?schema=public"

# Dev-only stub coach (Fase 1). Replaced by real session auth in Fase 3.
DEV_COACH_ID="coach-stub"

# Public URL of the web app (password-reset and email-verify links). Local demo: http://localhost:8765
APP_ORIGIN="http://localhost:8765"

# API public URL for OAuth callbacks when web and API are on different origins.
# Defaults to APP_ORIGIN when SERVE_WEB=true (same origin). Split deploy: e.g. http://localhost:3000
# API_ORIGIN="http://localhost:3000"

# Google OAuth (optional). Create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must match: ${API_ORIGIN or APP_ORIGIN}/auth/google/callback
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
# GOOGLE_OAUTH_STATE_SECRET=""  # optional; defaults to GOOGLE_CLIENT_SECRET

# Email (B0/D1). Default "console" logs to stdout in dev. Production: EMAIL_PROVIDER=google — real
# Gmail/Workspace SMTP adapter (App Password). The resend|postmark|ses branches are not implemented (they throw).
EMAIL_PROVIDER="console"
# Google SMTP (when EMAIL_PROVIDER=google). Needs 2FA on the account + an App Password
# (https://myaccount.google.com/apppasswords) — NOT the account password.
# GOOGLE_SMTP_USER=""           # sending mailbox, e.g. coach@your-domain.com
# GOOGLE_SMTP_APP_PASSWORD=""   # 16-char App Password
# EMAIL_FROM=""                 # optional From header, e.g. "Holy Oly <no-reply@your-domain.com>"; defaults to GOOGLE_SMTP_USER

# Billing (E3–E5/D2). Default "mock" for dev/demo. In PRODUCTION the app REFUSES TO START in mock mode
# (assertBillingProdConfig, so it can never silently charge $0): set BILLING_PROVIDER=mercadopago + all the MP vars below.
BILLING_PROVIDER="mock"
BILLING_WEBHOOK_SECRET="dev-mock-webhook-secret"

# Mercado Pago Chile (when BILLING_PROVIDER=mercadopago). Generate the 8 plan ids (4 tiers × 2 periods) with:
#   pnpm --filter @holy-oly/api exec tsx scripts/mp-setup-plans.ts   (prints the 8 lines ready to paste; --dry-run to preview)
# MERCADOPAGO_ACCESS_TOKEN=""
# MERCADOPAGO_WEBHOOK_SECRET=""
# MERCADOPAGO_PLAN_COACH_MONTHLY=""
# MERCADOPAGO_PLAN_COACH_SEMIANNUAL=""
# MERCADOPAGO_PLAN_PRO_MONTHLY=""
# MERCADOPAGO_PLAN_PRO_SEMIANNUAL=""
# MERCADOPAGO_PLAN_ELITE_MONTHLY=""
# MERCADOPAGO_PLAN_ELITE_SEMIANNUAL=""
# MERCADOPAGO_PLAN_BOX_MONTHLY=""
# MERCADOPAGO_PLAN_BOX_SEMIANNUAL=""

# Coach write gating: active subscription required (402). Off in tests unless BILLING_ENFORCE=true.
# Default on in dev/prod; set BILLING_ENFORCE=false to disable locally.
# BILLING_ENFORCE="false"

# Coach must verify email before confirming vínculos. Default on in dev/prod.
# EMAIL_VERIFY_ENFORCE="false"
