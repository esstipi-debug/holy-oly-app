{
  "name": "@holy-oly/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsup src/server.ts --format esm --clean --no-splitting",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:int": "vitest run --mode integration",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@holy-oly/core": "workspace:*",
    "@prisma/client": "^6.19.3",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "prisma": "^6.19.3",
    "tsup": "^8.5.1",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
