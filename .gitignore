# tooling / junk
.playwright-mcp/
.superpowers/
node_modules/
*.log
.DS_Store
Thumbs.db

# capturas sueltas de desarrollo (no son parte de la app; los íconos icon-*.png SÍ se versionan)
_mockup/shot-*.png
_mockup/cc-*.png
_mockup/c-top.png

dist/
*.tsbuildinfo

# env files with real secrets; .env.example IS versioned
.env
.env.*
!.env.example
# build-config env (no secrets — sets VITE_HASH_ROUTER for the single-file demo build)
!apps/web/.env.singlefile

# embedded-postgres ephemeral data dirs (db-verify orchestrator)
.pgdata*/

# Borrador de autoría de tips (huermn → wellnessTips): contiene texto de fuente premium —
# es scratch local para parafrasear a mano, JAMÁS se embarca ni se commitea.
docs/wellness/*-drafts.md
